apiVersion: v1
kind: Service
metadata:
  name: {{ include "Facticle-frontend.fullname" . }}
  labels:
    {{- include "Facticle-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Facticle-frontend.selectorLabels" . | nindent 4 }}
