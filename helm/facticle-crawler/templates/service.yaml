apiVersion: v1
kind: Service
metadata:
  name: {{ include "Facticle-crawler.fullname" . }}
  labels:
    {{- include "Facticle-crawler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Facticle-crawler.selectorLabels" . | nindent 4 }}
